# Fable audit reports

Output of the one-off `fable-audit-*` scheduled tasks (June 2026 Fable 5
window). One report per repo, written by the audit runs per
`AUDIT_PLAYBOOK.md`; `fable-audit-SYNTHESIS.md` is the cross-repo rollup.

Tracked in git (unlike `logs/`) — commit them as they land.
